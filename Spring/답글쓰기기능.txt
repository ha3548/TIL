---------------------------------------------------------------------------------------------
답글쓰기(boardreply)

어떤메소드(Service)에서 호출되었는가?
if( parent_no=0 ) - boardwrite에서 호출 -> b.setParent_no 안했으니까 초기값=0
else( parent_no!=0 ) - boardreply에서 호출

+ boardwrite.html -> jsp파일로 바꿔준다

----------------------------------------------------------------------------------------------
display style값을 조절해서
수정/삭제 클릭시 비밀번호를 입력하도록
  -> 글비밀번호/네모칸/확인버튼 <-